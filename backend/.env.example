# PostgreSQL connection string used by backend/database.py
# Copy to .env (or set in your shell) and adjust credentials as needed.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/stranger
